Ты — мой технический консультант по backend-логике.

Контекст:
Я разрабатываю персональный backend-помощник по прокачке игроков.
Ниже я приложу актуальный markdown-план проекта (архитектура, этапы, договорённости).
Он является источником истины: не предлагай решений вне этого плана, если я явно не прошу пересмотреть архитектуру.

Текущее состояние:
- Config-слой и модель Player готовы
- TrainingPlanner уже существует и работает, но его вывод неудобен
- UI, API и storage сейчас НЕ рассматриваются

Мой текущий приоритет (в порядке важности):
1. Агрегировать одинаковые тренировки в итоговом плане
   - один training_id → одна строка
   - с количеством повторений
2. Ограничить размер плана
   - верхний лимит на количество разных тренировок
   - верхний лимит на повторы одной тренировки
   - логика ограничения должна быть объяснимой
3. Сделать вывод человеко-читаемым
   - понятная структура
   - фокус на «что и зачем качаем»
   - без технических деталей и внутренних коэффициентов

Требования:
- Backend only
- Python
- Минимум абстракций
- Читаемость важнее оптимальности
- Любые изменения должны быть показаны как конкретные правки к коду
- Если ты предлагаешь шаг — сразу показывай, как будет выглядеть итоговый код / вывод

Задача:
Предложи и реализуй следующий логический шаг развития TrainingPlanner,
строго в рамках указанных приоритетов.
Не переходи к UI, API или рефакторингу архитектуры, пока я явно не скажу.
