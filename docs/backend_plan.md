# Детальный план реализации Backend помощника прокачки игроков

> Инструмент персональный, ориентирован на максимальную простоту и скорость разработки.  
> Backend пишется первым, без преждевременной оптимизации.  
> Проект развивается итеративно, с упором на читаемую и проверяемую логику.

---

## Легенда статусов

- 🟢 **Готово** — реализовано и используется
- 🟡 **Нужно исправить / улучшить** — работает, но логически неполно
- ⚪ **Нужно сделать** — запланировано, но не реализовано
- 🔴 **Удалить / заменить** — концептуально устарело

---

## Этап 0. Общие договорённости

- 🟢 Backend → Frontend → Интеграция → Доработки
- 🟢 Язык: **только русский** (UI, описания, конфиги)
- 🟢 Все идентификаторы: `snake_case`
- 🟢 Хардкод разрешён
- 🟢 Все справочники хранятся централизованно в `config/`
- 🟢 Минимум абстракций, читаемость важнее расширяемости
- 🟢 Инструмент строго для одного пользователя

---

## Этап 1. Config-слой (источник истины)

Config-слой не содержит логики, только данные.

### 1.1 `config/skills.py`

```python
SKILLS: dict[str, str] = {
    # Нападение
    "passing": "Передача",
    "dribbling": "Дриблинг",
    "cross": "Навес",
    "finishing": "Завершение",
    "shooting": "Удары",

    # Психофизика
    "physical": "Физическая форма",
    "strength": "Сила",
    "aggressiveness": "Агрессивность",
    "pace": "Скорость",
    "creativity": "Креативность",

    # Защита
    "tackling": "Отбор мяча",
    "marking": "Опека",
    "positioning": "Выбор позиции",
    "heading": "Игра головой",
    "bravery": "Храбрость",
}

Правила:

id навыка используется везде

после старта проекта id не меняются

значения навыков ∈ [1..400]

🟢 Статус: готово
🟡 Исправлено в процессе: верхний лимит навыков (400)

1.2 config/positions.py
Каждая позиция содержит белые навыки

Обновлено под новый список навыков с креативностью

Пример структуры:

POSITIONS = {
    "ST": {
        "name": "Нападающий",
        "white_skills": [
            "positioning",
            "heading",
            "passing",
            "dribbling",
            "shooting",
            "finishing",
            "strength",
            "pace",
            "creativity",
        ],
    },
    "AMC": {
        "name": "Атакующий полузащитник",
        "white_skills": [
            "heading",
            "passing",
            "dribbling",
            "shooting",
            "finishing",
            "physical",
            "pace",
            "creativity",
        ],
    },
}

🟢 Статус: готово

1.3 config/trainings.py

Каждая тренировка:

- влияет на фиксированный набор навыков
- содержит название на русском
- имеет тип: attack / defence / possession / physical

TRAININGS = {
    "stretching": {
        "name": "Растяжка",
        "type": "physical",
        "skills": ["strength", "pace", "physical"],
    },
}
🟢 Статус: готово

Этап 2. Domain-слой (чистая логика)
2.1 Модель игрока
class Player:
    id: int
    name: str
    positions: list[str]  # коды позиций
    skills: dict[str, int]  # skill_id -> значение

    @property
    def white_skills(self) -> set[str]:
        ...
    
    @property
    def gray_skills(self) -> set[str]:
        ...
    
    def weakest_white_skills(self, N: int) -> list[str]:
        ...

- Белые навыки — объединение по всем позициям
- Серые — все остальные

🟢 Статус: реализовано и используется

2.2 Аналитика тренировок (текущая логика)
Текущая реализация:
- ищет слабые белые навыки
- сортирует тренировки по:
- минимальному зацепу серых
- суммарной пользе белым

Проблемы:
- не строит план
- не симулирует рост
- не выравнивает навыки
- не учитывает лимит 400

🟡 Статус: работает, но логически недостаточно
🔴 В текущем виде подлежит замене

2.3 Месячная деградация
def apply_monthly_decay(player):
    for skill in player.skills:
        player.skills[skill] = max(1, player.skills[skill] - 20)
🟢 Статус: готово

Этап 3. Планировщик тренировок (ключевой этап)
Цель этапа
Сформировать рекомендацию вида:
«Сделай X таких тренировок, Y таких — и ты максимально выровняешь белые навыки»

Основные требования
- учитываются только белые навыки
- серые минимизируются
- навыки не превышают 400
- результат — план, а не список

3.1 Симуляция тренировки (виртуальная)
def simulate_training(skills: dict[str, int], training_id: str) -> dict[str, int]:
    """
    Возвращает новое состояние навыков
    без изменения оригинала
    """
⚪ Статус: нужно реализовать

3.2 План тренировок
@dataclass
class TrainingPlanItem:
    training_id: str
    name: str
    repeats: int
План:

агрегированный (без покадровой симуляции)
ориентирован на баланс (min/max белых)
⚪ Статус: нужно реализовать

3.3 Алгоритм планирования
Подход:
greedy + симуляция
на каждом шаге выбирается тренировка,
уменьшающая разброс белых навыков
Критерии:
минимальный рост максимального белого
максимальный рост минимального белого
штраф за серые навыки

🔴 Статус: отсутствует (ключевая задача)

Этап 4. Services

PlayerService

TrainingService

TrainingPlanService
Назначение:
связать domain и API
без DI и репозиториев
⚪ Статус: нужно сделать

Этап 5. Хранение данных

JSON / in-memory

один пользователь
один файл или структура
⚪ Статус: нужно сделать

Этап 6. API / Interface

Функциональность:

CRUD игрока
деградация
получение тренировочного плана
Примеры эндпоинтов:
GET /players
POST /players
PUT /players/{id}
POST /players/{id}/degrade
GET /players/{id}/training-plan
⚪ Статус: нужно реализовать


Итоговый статус проекта
Этап	Статус
Config (skills / positions / trainings)	🟢
Player model	🟢
Training analytics	🟡
Monthly decay	🟢
Training simulation	⚪
Training planner	🔴
Services	⚪
Storage	⚪
API	⚪
